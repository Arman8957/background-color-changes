import {useState} from 'react';

const pageChangingColors = () => {

const [color, setColor] = useState("red")

    return (
        <div className="w-full h-full duration-200 bg-slate-500 ">
           
        </div>
    );
};

export default pageChangingColors;